# GUI dashboard dependencies (in addition to the bapipe source in ../src).
#
# IMPORTANT: use Python 3.11. The bapipe library targets the pandas 1.x API
# (groupby(axis=1), stack(dropna=...)), so we pin pandas 1.5.x + numpy 1.x.
# Newer Python/pandas break the library at load time.
#
#   python3.11 -m venv .venv && source .venv/bin/activate
#   pip install -r gui_app/requirements.txt

numpy==1.26.4
pandas==1.5.3
scipy==1.11.4
scikit-image==0.22.0
matplotlib==3.8.4
seaborn==0.13.2
opencv-python-headless==4.10.0.84
Pillow>=9,<12.4  # jpeg figure export via matplotlib
tables==3.11.1
imageio[ffmpeg]==2.34.2
ffmpeg-python==0.2.0
Click==8.1.7
tqdm
streamlit
bcrypt>=4  # email/password hashing when auth is enabled
Authlib>=1.3.2  # required for Google (OIDC) login when auth is enabled
httpx>=0.27  # required by Authlib starlette client for Google (OIDC) login
streamlit-image-coordinates  # click-to-set arena corners in the wizard
